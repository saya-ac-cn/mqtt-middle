<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ac.cn.saya.mqtt.middle.repository.primary.IotStandardDAO">

    <!-- 查询所有的物理量-->
    <select id="findAll" resultType="iotStandardUnit">
        select `id`,`name`,`symbol` from iot_standard_unit
    </select>

</mapper>